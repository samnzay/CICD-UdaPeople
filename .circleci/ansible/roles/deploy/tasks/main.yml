---

  - name: "Artifact extration"
    unarchive:
      src: artifact.tar.gz
      dest: .

  - name: "Start the app"
    shell: |

      npm install
      pm2 stop default
      pm2 start npm -- start
    